<div class="chat-container">
  <h1>ChatGPT</h1>
  <section class="chatgpt-conversation"></section>

  <%= form_with do |form| %>
    <div class="chat-input">
      <%= form.text_area :message, id: 'prompt' %>
      <%= form.submit 'send' %>
    </div>
  <% end %>
</div>